# --- Framework web ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# --- Configuración ---
pydantic==2.7.4
pydantic-settings==2.3.4
email-validator==2.2.0

# --- Bases de datos ---
sqlalchemy==2.0.31
pymysql==1.1.1
cryptography==42.0.8          # también usado para Fernet (encriptación de credenciales)
pymongo==4.8.0
redis==5.0.7

# --- Autenticación / seguridad ---
python-jose[cryptography]==3.3.0
passlib==1.7.4
# passlib 1.7.4 es incompatible con bcrypt >= 4.1 (ValueError en la detección de
# backend). Fijamos bcrypt 4.0.1, que funciona con passlib.
bcrypt==4.0.1

# --- Integraciones externas (Gmail / Dropbox) ---
httpx==0.27.0
dropbox==11.36.2
google-api-python-client==2.137.0
google-auth==2.32.0

# --- Testing ---
pytest==8.2.2
pytest-cov==5.0.0
