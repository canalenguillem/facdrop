"""Cliente Redis — FASE 2.

Placeholder de Fase 1. Aquí se inicializará el cliente de Redis a partir de
`REDIS_URL` (cache / colas / rate limiting).
"""
# import redis
# from app.config import settings
#
# redis_client = redis.from_url(settings.REDIS_URL, decode_responses=True)
